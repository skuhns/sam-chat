{
  "name": "sam-chat",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace @sam-chat/web",
    "lint": "npm run lint --workspaces",
    "format": "npm run format:write --workspaces"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
